{
  "name": "vimfields",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:dom": "vitest run tests/dom/"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.1.0",
    "jsdom": "^26.0.0",
    "@vitest/coverage-v8": "^3.1.0"
  }
}
